<!DOCTYPE html>
<html>
  <head>
    <title>ChatApp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% if current_user.try(:admin?) %>
      <h2>Let me fill the gap in your soul.</h2>
      <div><%= current_user.name %>さん(管理者)</div>
      <li><%= link_to "管理", rails_admin_path %></li>
      <li><%= link_to "マイページ", client_path(current_user.id) %></li>
      <li><%= link_to "チャットルーム一覧", rooms_path %></li>
      <li><%= link_to "ログアウト", destroy_user_session_path, method: :delete %></li>
    <% elsif current_user %>
      <h2>Let me fill the gap in your soul.</h2>
      <div><%= current_user.name %>さん</div>
      <li><%= link_to "マイページ", client_path(current_user.id) %></li>
      <li><%= link_to "チャットルーム一覧", rooms_path %></li>
      <li><%= link_to "ログアウト", destroy_user_session_path, method: :delete %></li>
      <% else %>
    <% end %>
    <%= yield %>
    <footer>

    </footer>
  </body>
</html>
